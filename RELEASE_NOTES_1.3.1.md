# KiCad Track Gloss 1.3.1 — less-DRC test build

- Certifies exact copper-equivalent rewrites without launching KiCad DRC.
- Retains the existing strict copper-removal certificate where it is valid.
- Tries a composed fixed-endpoint connection core alongside the broad candidate.
- Keeps single-net geometric search and fixed-point operators unchanged.

This is an intermediate test build from the `less_drc` branch. It has only
been checked with the bounded 60-second CLI scenario requested for
`muzy_zynq4`; the full non-regression suite is intentionally deferred.
